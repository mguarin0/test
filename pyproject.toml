[build-system]
requires = [
    "setuptools>=61",
    "wheel",
    "pybind11[global]>=2.10",
    "cmake>=3.18",
    "ninja"
]
build-backend = "setuptools.build_meta"

[project]
name = "vector_add"
version = "0.1.0"
description = "JAX extension with custom CUDA kernel"
requires-python = ">=3.8"
