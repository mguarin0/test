[build-system]
requires = [
    "setuptools>=61.0",
    "wheel",
    "pybind11[global]>=2.10",
    "cmake>=3.18",
    "ninja"  # optional
]
build-backend = "setuptools.build_meta"

[project]
name = "vector_add"
version = "0.1.0"
description = "A JAX FFI + CUDA extension"
requires-python = ">=3.8"
